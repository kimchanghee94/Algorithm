#include <string>
#include <vector>
using namespace std;

#define MAX 55

int flag[MAX][MAX];
int di[4] = { 1, 0, -1, 0 }, dj[4] = { 0, -1, 0, 1 };

int solution(vector<vector<int>> game_board, vector<vector<int>> table) {
	int answer = -1;
	return answer;
}